{
  "name": "academy-telegram-relay",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "deploy:dry": "wrangler deploy --dry-run"
  },
  "devDependencies": { "wrangler": "^4.141.0" }
}
