# Текущая задача

## [ПОДТВЕРЖДЕНО] Этап 06 — очередь разборов и сигналы

```text
AS-OWNER-RAZBOR-SALES-MVP__06_RAZBOR_QUEUE_AND_ALERTS
```

- Создан лист `Очередь_Разборов`.
- Предразбор запускается после `qualification_completed`; `owner_slot_booked` сохранён fallback.
- Создан активный watcher `Q05uFJYgb8Zstodm`, период 15 минут, Telegram Trigger отсутствует.
- No-slots переводит собственника в `waiting_for_slot`; появление слота — в `slots_offered`; бронь — в `booking_created`.
- Live Test D подтвердил одну бронь и отсутствие дублей `AI_Предразбор` / `Графики_Теста`.
- Кнопки должны отображаться как `дата, день, время`, без имени эксперта. Итоговое проверенное значение: `20.07, пн, 12:00 МСК`.
- Сообщение с использованной slot-кнопкой удаляется после выбора.

Отчёт:

```text
06_REPORTS/AS-OWNER-RAZBOR-SALES-MVP__06_RAZBOR_QUEUE_AND_ALERTS_REPORT.md
```

## [ЕСТЬ РИСК]

- Итоговая подпись кнопки после последнего исправления проверена на реальной строке и live-коде, но повторно в Telegram не отправлялась.
- Старые ненажатые slot-сообщения пока автоматически не собираются; удаляется сообщение, на котором сделан выбор.
- Booking-path остаётся медленным: последний live execution `10,978 секунды`.

## [ОДИН СЛЕДУЮЩИЙ ШАГ]

Отдельно спроектировать cleanup старых ненажатых slot-сообщений без замедления клиентского ответа.

## [ПОДТВЕРЖДЕНО] Закрытый этап FAST RESPONSE

```text
AS-OWNER-RAZBOR-SALES-MVP__03B_OWNER_FAST_RESPONSE
```

Фактически измеренный SLA `4,849 секунды` принят владельцем проекта как временный MVP.

Data Tables на следующем этапе не внедрять.

## [ТЕКУЩИЙ ЭТАП]

```text
AS-OWNER-RAZBOR-SALES-MVP__04_OWNER_SLOT_SELECTION
```

Цель: после 5-го вопроса квалификации показать свободные слоты активных экспертов и безопасно создать запись на разбор.

## [ПОДТВЕРЖДЕНО] Результат первичного аудита

- OWNER parent `xLcyZ78h78bhncPa` active, 28 узлов, один Telegram Trigger.
- OWNER chat `mBaJjoCgCyEFwjSy` active, 41 узел, Telegram Trigger отсутствует.
- Async logging `dVHYHMrk7G31Ubzk` active, Telegram Trigger отсутствует.
- `Эксперты`: 0 строк.
- `Слоты_Разборов`: 0 строк.
- `Записи_Разборов`: 0 строк.
- Все 8 message keys этапа `04` отсутствуют в `Сообщения_OWNER`.
- Схема Google Sheets `read free → update → reread` не является атомарной защитой от двойного бронирования.

## [ЗАПРЕТ]

- не внедрять Data Tables;
- не менять AS-BOT__00_MAIN_ROUTER, Batman, HR-Zoom и platform webhook;
- не подключать AI, оплату и платформу;
- не создавать второй Telegram Trigger;
- не менять credentials, названия листов и колонок;
- не удалять тестовые строки без разрешения;
- не внедрять небезопасное бронирование без lock-механизма.

## [ПОДТВЕРЖДЕНО] Минимальный lock-слой

Владелец проекта утвердил отдельный лист:

```text
Блокировки_Слотов
```

Лист создан 2026-07-15 с колонками `lock_id`, `created_at`, `expires_at`, `slot_id`, `owner_id`, `booking_id`, `status`, `execution_id`, `comment`.

Перед созданием сохранены live-backup OWNER parent, OWNER chat, async logging и полные копии семи рабочих листов. После записи подтверждено: прежние листы и проверяемые live-workflow не изменились.

## [ПОДТВЕРЖДЕНО] Тексты выбора слотов

В `Сообщения_OWNER` добавлены 8 обязательных message keys этапа и отдельный редактируемый ключ `slot_selection_button`. Все 9 строк имеют статус `черновик`. Workflow не менялись.

Основные клиентские тексты хранятся только на листе. Универсальный fallback `Сообщение временно недоступно.` отменён решением владельца проекта. При будущем patch workflow для каждого шага должен использоваться короткий смысловой аварийный ответ, который продолжает воронку.

## [СЛЕДУЮЩИЙ ШАГ]

Тест D закрыт. Создать изолированные тестовые данные эксперта и слотов, видимые только `owner_000001`, затем провести тесты A/B/C.

## [ИЗМЕНЕНО] OWNER slot-selection funnel

Дата patch: 2026-07-15.

- существующий OWNER Telegram Trigger принимает `message` и `callback_query`; второй Trigger не создан;
- после пятого вопроса используется `qualification_to_slot_selection` и кнопка из `slot_selection_button`;
- эксперты и слоты читаются только после запроса выбора времени;
- показываются до 6 подходящих слотов не раньше следующего дня;
- бронирование использует append-only lock в `Блокировки_Слотов`, повторное чтение слота и post-write verification;
- запись создаётся в `Записи_Разборов`, слот получает `booked`, собственник — `разбор_забронирован`;
- основной текст берётся из `Сообщения_OWNER`; аварийные ответы зависят от текущего шага.

Backup непосредственно перед patch:

```text
03_N8N_WORKFLOWS/_LIVE_BACKUP_BEFORE_OWNER_SLOT_SELECTION/20260715_203502/
04_GOOGLE_SHEETS/_BACKUP_BEFORE_OWNER_SLOT_SELECTION/20260715_203502/
```

После patch подтверждено: OWNER parent и OWNER chat активны; Telegram Trigger один; async logging, Batman и platform workflow не изменились.

## [ПОДТВЕРЖДЕНО] Live-тест D — свободных слотов нет

Дата: 2026-07-15.

```text
parent execution: 418, success, 130 ms
OWNER chat execution: 419, success, 5 511 ms
async execution: 420, success, 5 043 ms
```

- клиент получил `slots_not_available` из `Сообщения_OWNER`;
- действие `owner_slots_none` выполнено;
- событие `owner_no_slots_available` записано в `Логи` со статусом `success`;
- в OWNER-группу отправлены два служебных зеркала;
- посторонних Telegram-получателей нет;
- ошибок execution нет.

Перед повторным тестом исправлен формат дополнительного async-события. Backup:

```text
03_N8N_WORKFLOWS/_LIVE_BACKUP_BEFORE_OWNER_SLOT_SELECTION/20260715_205305/
04_GOOGLE_SHEETS/_BACKUP_BEFORE_OWNER_SLOT_SELECTION/20260715_205305/
```

## [РИСК]

Ветка чтения слотов выполнялась `5,511 секунды`, что на `0,662 секунды` выше принятого временного SLA `4,849 секунды`. Data Tables и дополнительную оптимизацию на этом этапе не внедрять согласно решению владельца проекта.
