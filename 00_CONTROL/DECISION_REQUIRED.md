# DECISION REQUIRED

[CODEX→CHATGPT]

[ЭТАП] `AS-SMART-LINK-ROUTER-MVP-001`

Подготовка локального MVP завершена. До любого подключения нужны четыре явных решения.

## [РЕШЕНИЕ 1] SITE_BASE_URL

Нужен точный production HTTPS URL сайта без `/index.html`, query и fragment.

Формат ответа: `SITE_BASE_URL = https://...`

## [РЕШЕНИЕ 2] Место для event-log

Нужно выбрать отдельную Google Spreadsheet и заранее созданный лист. Не использовать HR-таблицу или боевой лист без отдельного архитектурного решения.

Также требуется определить срок хранения `user_agent` и круг доступа к журналу.

## [РЕШЕНИЕ 3] Разрешение на deploy Web App

Сейчас deploy запрещён. Требуется отдельное явное разрешение с выбранным владельцем Apps Script и режимом доступа Web App. В разрешении также нужно подтвердить приемлемость дополнительного клика `Продолжить`: Apps Script `HtmlService` не даёт надёжный серверный HTTP 302.

## [РЕШЕНИЕ 4] Разрешение на тест одной строки hr_invite

Сейчас любые изменения строк запрещены. После deploy потребуется отдельное явное разрешение на тест ровно одной заранее выбранной строки `hr_invite`, с backup, ожидаемым результатом и rollback.

## [НЕ ТРЕБУЕТСЯ И НЕ РАЗРЕШЕНО]

- generator diff не применять;
- 3000 строк не менять;
- production генератор, `business_test_main`, `AS-BOT__00_MAIN_ROUTER`, n8n, Telegram и HR-форму не трогать.

## [ОДИН СЛЕДУЮЩИЙ ШАГ]

ChatGPT передаёт только решения 1 и 2: утверждённый `SITE_BASE_URL` и выбранное отдельное место event-log. Решения 3 и 4 принимаются позднее и отдельно.
